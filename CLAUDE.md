# CLAUDE.md — קרא אותי ראשון, בכל סשן

מערכת דוחות בטיחות בעבודה לממונה בטיחות — אתרי בנייה ומפעלים.
ספריית הבדיקה נגזרת מקורפוס החקיקה הישראלי שבריפו.

## צעד 0 — לפני כל פעולה

1. קרא את [`docs/00-INDEX.md`](docs/00-INDEX.md) — מפת כל המסמכים.
2. קרא את [`TODO.md`](TODO.md) — מה פתוח עכשיו.
3. קרא את קובץ ה-doc הרלוונטי למשימה שלך. אל תתחיל לכתוב קוד בלי זה.

## שלושת הכללים שגוברים על הכל

**1. הספרייה נגזרת מהחוק — לא מועתקת ולא מומצאת.**
כל סעיף בדיקה נגזר מנוסח ב-`data/legislation/`, נושא `scope` שמפנה
אליו, שומר `source_excerpt` מתוכו, ואושר על ידי ממונה בטיחות.

**אל תעתיק ספריית בדיקה ממערכת אחרת, ואל תמציא סעיפים מהזיכרון.**
סעיף בלי `reviewed_by` נחסם ב-CI.
ראה [`docs/04-CONTENT-LIBRARY.md`](docs/04-CONTENT-LIBRARY.md)
ו-[`docs/13-LIBRARY-BUILD.md`](docs/13-LIBRARY-BUILD.md).

**2. נגישות היא תנאי merge, לא משימה בסוף.**
אין PR שעובר בלי `axe` נקי ברמת AA. הסיבה אינה אידיאולוגית: סעיף 19נא
לחוק שוויון זכויות קובע 50,000 ₪ לכל מחדל בלי הוכחת נזק, ותובענה ייצוגית
בנגישות פטורה מאגרה. ראה [`docs/06-ACCESSIBILITY.md`](docs/06-ACCESSIBILITY.md)
ו-[`docs/07-LEGAL-COMPLIANCE.md`](docs/07-LEGAL-COMPLIANCE.md).

**3. מצב אחד, מקור אמת אחד.**
כל מסך נגזר ממקור אמת יחיד. אין ספירה מקומית, אין מונה עצמאי —
אחרת מסכים מדווחים מספרים סותרים על אותו דוח.
ראה [`docs/03-STATE-MACHINE.md`](docs/03-STATE-MACHINE.md).

## הכלל הרביעי — AI לא מאשר תקינות

חילוץ אוטומטי רשאי להציע **ליקוי**. לעולם לא "תקין" ולא "קיים".
הצעת ליקוי שגויה עולה בדיקה מיותרת; הצעת תקינות שגויה עולה בכך שמפגע
אמיתי לא נבדק. SafeSite מקיימת את שלוש תכונות "השילוש הקטלני", ולכן
ההגנה חייבת להיות מבנית ולא מבוססת זיהוי.
ראה [`docs/25-AI-SECURITY.md`](docs/25-AI-SECURITY.md).

## מה לא לעשות

- אל תוסיף תלות שנטענת מ-CDN חיצוני בזמן ריצה. הכל ב-bundle.
- אל תשאיר `console.log` או `TODO` בקוד שנדחף ל-main.
- אל תסמן משימה כהושלמה בלי שהבדיקה שלה עוברת.
- אל תמחק או תעקוף בדיקת נגישות כדי ש-CI יעבור.
- אל תשתמש לעולם ב-`--dangerously-skip-permissions`.
- אל תוסיף תלות לא נעוצה, ואל תריץ `npx` על חבילה בלי גרסה.

## הקשר משתמש

מוטי — עברית RTL היא אזרח ראשון, לא תרגום. כל טקסט במוצר בעברית.
תגובות ותיעוד בעברית. שמות משתנים, קבצים וקומיטים באנגלית.

## סטטוס

הפרויקט בשלב 1 — גל ראשון של הספרייה. אין עדיין אפליקציה; המחסנית
(Next.js 15 + Supabase) קמה בשלב 2.

חולצו החובות מ-2.2 ומ-2.1 ל-`data/obligations/` בעזרת
`scripts/extract-obligations.ts`. פקודות (pnpm 9.12, כל התלויות נעוצות):
`pnpm test` · `pnpm typecheck` · `pnpm library:extract` · `pnpm library:check` ·
`pnpm legislation:verify`.
**המועמדים אינם סעיפי בדיקה** — הבא בתור הוא ניסוח טיוטה, ואחריו
אישור ממונה בטיחות, שחוסם ייצור.
**הצעד הבא תמיד ב-[`docs/26-EXECUTION-PLAN.md`](docs/26-EXECUTION-PLAN.md)** וב-`TODO.md`.
